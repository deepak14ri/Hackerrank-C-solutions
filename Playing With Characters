#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() 
{
    char ch[100],s[100],sen[100];
    scanf("%c", &ch);
    scanf("%s", &s);
    fgets(sen,sizeof(100),stdin);
    scanf("%[^\n]%*c", &sen);
    printf("%s\n", ch);
    printf("%s\n",s); 
    puts(sen);
    return 0;
}
